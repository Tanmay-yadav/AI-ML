# Concurrency Cheat Sheet\n\n## Threading\n```python\nimport threading\nimport time\n\ndef worker():\n    print(f\"Thread: {threading.current_thread().name}\")\n\n# Create and start thread\nt = threading.Thread(target=worker, name=\"Worker-1\")\nt.start()\nt.join()  # Wait for thread\n\n# Thread with arguments\ndef greet(name):\n    print(f\"Hello, {name}\")\n\nt = threading.Thread(target=greet, args=(\"Alice\",))\nt.start()\n```\n\n## Locks and Synchronization\n```python\nimport threading\n\nlock = threading.Lock()\n\ndef critical_section():\n    with lock:\n        # Thread-safe code\n        pass\n\n# Semaphore\nsem = threading.Semaphore(3)  # Allow 3 threads\nwith sem:\n    # Only 3 threads can run here\n    pass\n\n# Event\nevent = threading.Event()\nevent.set()        # Signal\nevent.wait()       # Wait for signal\nevent.clear()      # Clear signal\n```\n\n## Thread Pool\n```python\nfrom concurrent.futures import ThreadPoolExecutor\n\ndef process(item):\n    return item * 2\n\nwith ThreadPoolExecutor(max_workers=4) as executor:\n    results = executor.map(process, [1, 2, 3, 4, 5])\n    for result in results:\n        print(result)\n\n# Submitting individual tasks\nwith ThreadPoolExecutor(max_workers=4) as executor:\n    future = executor.submit(process, 5)\n    result = future.result()  # Wait for result\n```\n\n## Multiprocessing\n```python\nfrom multiprocessing import Process, Pool\nimport os\n\ndef worker(x):\n    print(f\"Process {os.getpid()}: {x}\")\n    return x * 2\n\n# Create process\np = Process(target=worker, args=(5,))\np.start()\np.join()\n\n# Process pool\nwith Pool(4) as pool:\n    results = pool.map(worker, [1, 2, 3, 4, 5])\n    print(results)\n\n# Process with return value\nwith Pool(4) as pool:\n    result = pool.apply(worker, (5,))\n    print(result)\n```\n\n## Queues\n```python\nfrom queue import Queue, LifoQueue, PriorityQueue\nimport threading\n\nq = Queue()\n\ndef producer():\n    for i in range(5):\n        q.put(i)\n\ndef consumer():\n    while True:\n        item = q.get()\n        if item is None:\n            break\n        print(f\"Processing {item}\")\n        q.task_done()\n\np = threading.Thread(target=producer)\nc = threading.Thread(target=consumer)\np.start()\nc.start()\n\nq.put(None)  # Signal to stop\np.join()\nc.join()\n```\n\n## Asyncio\n```python\nimport asyncio\n\nasync def async_function():\n    print(\"Starting\")\n    await asyncio.sleep(1)\n    print(\"Done\")\n    return \"Result\"\n\n# Run coroutine\nasyncio.run(async_function())\n\n# Multiple coroutines\nasync def main():\n    results = await asyncio.gather(\n        async_function(),\n        async_function(),\n        async_function()\n    )\n    return results\n\nasyncio.run(main())\n\n# Create task\nasync def main():\n    task = asyncio.create_task(async_function())\n    result = await task\n    return result\n```\n\n## Async Context Managers and Iterators\n```python\nimport asyncio\n\nclass AsyncResource:\n    async def __aenter__(self):\n        print(\"Acquiring resource\")\n        await asyncio.sleep(0.1)\n        return self\n    \n    async def __aexit__(self, *args):\n        print(\"Releasing resource\")\n        await asyncio.sleep(0.1)\n\nasync def main():\n    async with AsyncResource() as resource:\n        print(\"Using resource\")\n\nasyncio.run(main())\n```\n\n## GIL (Global Interpreter Lock)\nThe GIL prevents multiple threads from executing Python bytecode simultaneously:\n- **Use threading** for I/O-bound tasks (network, file I/O)\n- **Use multiprocessing** for CPU-bound tasks\n- **Use asyncio** for I/O-bound tasks with single process\n\n## Race Conditions\n```python\nimport threading\n\nshared_variable = 0\nlock = threading.Lock()\n\ndef increment():\n    global shared_variable\n    with lock:  # Prevent race condition\n        temp = shared_variable\n        temp += 1\n        shared_variable = temp\n\nthreads = [threading.Thread(target=increment) for _ in range(10)]\nfor t in threads:\n    t.start()\nfor t in threads:\n    t.join()\n\nprint(shared_variable)  # Should be 10\n```\n"